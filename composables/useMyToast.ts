import type { ToastOptions } from "~/types/toast";

export default () => {
  const toast = useToast()
  function sendToast(option:ToastOptions){
    
  }

  return {
    //data
    //methods
    sendToast,
  };
};